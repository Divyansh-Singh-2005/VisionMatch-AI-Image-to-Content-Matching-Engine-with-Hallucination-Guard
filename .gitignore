# Secrets
.env
*.key

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Editors / OS
.vscode/
.idea/
Thumbs.db
.DS_Store

# Runtime artifacts
logs/
*.log
pgdata/

# v2 corpus (rebuilt from data/v2/manifest.jsonl.gz)
data/v2/images/
*.part

# v2 CLIP outputs (regenerable); only predictions + summaries are committed
data/v2/clip/smoke/
data/v2/clip/**/parts/
data/v2/clip/**/*.npz

# audit working files (the gzipped results + summary are committed)
data/v2/audit/results.jsonl
